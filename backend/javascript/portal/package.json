{
  "name": "portal-backend",
  "version": "1.0.0",
  "description": "API backend — sirve /api/cv, /api/cv.pdf, /api/ai/ask (proxy), /health",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch --experimental-strip-types src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "mqtt": "^5.15.1",
    "pdfkit": "^0.17.2",
    "ws": "^8.18.1"
  },
  "devDependencies": {
    "@types/node": "^22.19.17",
    "@types/pdfkit": "^0.17.3",
    "@types/ws": "^8.5.14",
    "typescript": "^5.0.0"
  }
}
