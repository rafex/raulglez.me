apiVersion: v1
kind: Service
metadata:
  name: {{ include "portal-publico.fullname" . }}
  labels:
    {{- include "portal-publico.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "portal-publico.selectorLabels" . | nindent 4 }}
