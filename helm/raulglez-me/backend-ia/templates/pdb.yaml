{{- if .Values.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "backend-ia.fullname" . }}
  labels:
    {{- include "backend-ia.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.pdb.minAvailable }}
  selector:
    matchLabels:
      {{- include "backend-ia.selectorLabels" . | nindent 6 }}
{{- end }}
