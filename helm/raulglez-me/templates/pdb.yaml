{{- if .Values.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "raulglez-me.fullname" . }}
  labels:
    {{- include "raulglez-me.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.pdb.minAvailable }}
  selector:
    matchLabels:
      {{- include "raulglez-me.selectorLabels" . | nindent 6 }}
{{- end }}
