apiVersion: v1
kind: Service
metadata:
  name: {{ include "raulglez-me.fullname" . }}
  labels:
    {{- include "raulglez-me.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "raulglez-me.selectorLabels" . | nindent 4 }}
