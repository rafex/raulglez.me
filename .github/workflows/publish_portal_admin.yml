name: Publish Portal Admin

on:
  push:
    branches: [main]
    paths:
      - frontend/portal-admin/**
      - containers/Dockerfile.portal-admin
      - containers/nginx-portal-admin.conf
      - containers/nginx-main.conf
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'

      - name: Build portal-admin
        run: |
          cd frontend/portal-admin
          npm ci
          npm run build

      - name: Set image name
        id: meta
        run: echo "image=ghcr.io/${{ github.repository_owner }}/raulglez-portal-admin:${{ github.ref_name == 'main' && 'latest' || github.ref_name }}" >> $GITHUB_OUTPUT

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: containers/Dockerfile.portal-admin
          push: true
          tags: ${{ steps.meta.outputs.image }}
