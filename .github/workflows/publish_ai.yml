name: Publish AI

on:
  push:
    branches: [main]
    paths:
      - backend/javascript/ia/**
      - backend/python/ia/**
      - containers/Dockerfile.backend-ia
      # cv.json ya NO está en la imagen — se obtiene vía HTTP al arrancar
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Set image name
        id: meta
        run: echo "image=ghcr.io/${{ github.repository_owner }}/raulglez-backend-ia:${{ github.ref_name == 'main' && 'latest' || github.ref_name }}" >> $GITHUB_OUTPUT

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: containers/Dockerfile.backend-ia
          push: true
          tags: ${{ steps.meta.outputs.image }}
